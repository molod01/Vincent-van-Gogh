import React from 'react';
import PropTypes from 'prop-types';


const Layout = () => (
  <div>
    Layout Component
  </div>
);

Layout.propTypes = {};

Layout.defaultProps = {};

export default Layout;
